import type { Ally, CommunityEvent, GalleryItem, Trip, Video } from "@/types/content";

/* Colecciones reales. Están vacías a propósito: se llenarán cuando exista
 * contenido verificable. La interfaz ya está preparada para mostrarlas. */

export const videos: Video[] = [];

export const events: CommunityEvent[] = [];

export const trips: Trip[] = [];

export const gallery: GalleryItem[] = [];

export const allies: Ally[] = [];
